/**
 * Thank-you-pages specific reveals.
 *
 * For simple cases prefer adding `data-dc-animate="..."` directly in
 * Webflow and let `initAutoReveal()` handle it. Use this file for
 * bespoke sequences (composed timelines, custom triggers, etc.) that
 * cannot be expressed declaratively.
 */
export function initThankYouAnimations(): void {
  // Add thank-you-specific reveals here.
}
