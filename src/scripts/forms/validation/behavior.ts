/**
 * Centralized config: should validation error messages be auto-translated?
 *
 * true  = Error messages are shown in the browser's (email) or selected country's (phone) language.
 * false = Error messages are always shown in English.
 */
export const USE_ERROR_AUTO_TRANSLATION = false;
